# The function ord takes a string argument of a single Unicode character and returns its integer Unicode point value
ascii_code = ord('c')
print(ascii_code)

# The function chr takes an integer argument and returns the string representing a character at that code point
int_to_ascii = chr(86)
print(int_to_ascii)